package crons

func Starter() {

	// c := cron.New(cron.WithLocation(time.Local))

	// // ทำงานตอนเที่ยงคืน 1 นาที
	// c.AddFunc("1 0 * * *", func() {
	// 	time.Sleep(time.Millisecond * 100)
	// 	// start
	// })

	// // Start
	// c.Start()
	// logx.Infof(` > Cron jobs, Started.`)

}
